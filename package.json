{
  "name": "hostel-admin-portal",
  "version": "1.0.0",
  "description": "Admin-only hostel room registration system with Firebase integration",
  "main": "index.html",
  "scripts": {
    "start": "http-server -p 8000 -c-1",
    "dev": "http-server -p 3000 -c-1 -o",
    "build": "echo 'No build process needed for static files'",
    "test": "echo 'No tests specified'"
  },
  "keywords": [
    "hostel",
    "admin",
    "firebase",
    "registration",
    "student-management"
  ],
  "author": "Hostel Admin System",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "repository": {
    "type": "git",
    "url": "."
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
